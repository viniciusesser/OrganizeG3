import {
    NavigationContent,
} from "@/app/navigation/NavigationContent";

export function DesktopNavigation() {
    return (
        <nav
            aria-label="Módulos do sistema"
            className="og3-navigation"
        >
            <NavigationContent />
        </nav>
    );
}